<%= simple_form_for(@comment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :commenter %>
    <%= f.input :body %>
    <%= f.association :pin %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
